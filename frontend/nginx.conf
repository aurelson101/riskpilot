server {
    listen 8080;
    server_name _;
    root /usr/share/nginx/html;

    location / {
        # Do not probe directories: the SPA route /assets would otherwise
        # collide with Vite's /assets directory and redirect outside HTTPS.
        try_files $uri /index.html;
    }
}
